const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado' // função Arrow usada

console.log(resultado(7.1))
console.log(resultado(6.1))

const nota2 = 2

const status = (nota2 >= 7 ? 'Aprovado' : 'Reprovado')

console.log(status)